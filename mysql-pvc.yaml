apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-pvc
  namespace: mysql
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: standard  # Minikube nutzt "standard" als default
  resources:
    requests:
      storage: 10Gi
