apiVersion: v1
kind: Secret
metadata:
  name: mysql-secret
  namespace: mysql
type: Opaque
stringData:
  MYSQL_ROOT_PASSWORD: "your-secure-password-here"  # ⚠️ Ändere dies!
  MYSQL_DATABASE: "myapp"
  MYSQL_USER: "myapp_user"
  MYSQL_PASSWORD: "myapp-password"  # ⚠️ Ändere dies!
