apiVersion: v1
kind: Secret
metadata:
  name: mysql-secret
  namespace: mysql
type: Opaque
stringData:
  MYSQL_ROOT_PASSWORD: "root"  # ⚠️ Ändere dies!
  MYSQL_DATABASE: "dev"
  MYSQL_USER: "dev"
  MYSQL_PASSWORD: "devuser"  # ⚠️ Ändere dies!
